@model List<Category>

<div class="row">
    <div class="col-md-12">
        <h6 class="display-4">Admin Categories Managament</h6>
        <hr />
        <a href="/admin/categories/create" class="btn btn-sm btn-primary">Create Category</a>
        <table class="table table-bordered mt-2">
            <thead>
                <tr>
                    <th width="30">ID</th>
                    <th>Name</th>
                    <th width="140">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count > 0)
                {
                    @foreach (var category in Model)
                    {
                <tr>
                    <td>@category.CategoryId</td>
                    <td>@category.Name</td>
                    <td>
                        <a href="/admin/categories/@category.Name" class="btn btn-sm btn-primary mr-1">Edit</a>
                        <form action="/Admin/CategoryDelete" method="post" class="d-inline">
                            <input type="hidden" name="categoryId" value="@category.CategoryId" />
                            <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                        </form>
                    </td>
                </tr>}}
                else
                {
                    //ÜRün olmadığında yapılacak işlem
                }

            </tbody>
        </table>
    </div>
</div>